[package]
name = "herdr-mission-control"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Tiled overview of every pane in the current workspace, for herdr"

[dependencies]
ratatui = "0.30"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
